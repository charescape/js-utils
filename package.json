{
  "name": "@charescape/js-utils",
  "version": "1.0.5",
  "description": "Lightweight javascript utilities for node.js and web",
  "keywords": [
    "javascript",
    "utilities"
  ],
  "main": "dist/js-utils.cjs.js",
  "module": "dist/js-utils.esm.js",
  "types": "types/index.d.ts",
  "files": [
    "src/",
    "dist/",
    "types/"
  ],
  "author": "Charescape <charescape@outlook.com>",
  "repository": {
    "type": "git",
    "url": "https://github.com/charescape/js-utils"
  },
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist/ && rollup -c",
    "dev": "rollup -c -w",
    "pretest": "npm run build",
    "cypress:open": "cypress open",
    "test": "npm run test:node && npm run test:jsdom",
    "test:node": "jest   --env=node --errorOnDeprecated --coverage --coverageProvider=v8 --verbose",
    "test:jsdom": "jest --env=jsdom --errorOnDeprecated --coverage --coverageProvider=v8 --verbose",
    "redo-install": "rm -rf node_modules/ && rm -f package-lock.json && npm install --registry=https://registry.npmjs.org -dd",
    "do-npm-login": "npm login --registry=https://registry.npmjs.org -dd",
    "do-npm-publish": "npm publish . --access public --registry=https://registry.npmjs.org -dd"
  },
  "devDependencies": {
    "@rollup/plugin-typescript": "^8.2.1",
    "@tsconfig/recommended": "^1.0.1",
    "@types/jest": "^26.0.22",
    "@types/qs": "^6.9.6",
    "axios": "^0.21.1",
    "cypress": "^7.1.0",
    "jest": "^26.6.3",
    "lodash-es": "^4.17.21",
    "rollup": "^2.45.0",
    "rollup-plugin-terser": "^7.0.2",
    "tslib": "^2.2.0",
    "typescript": "^4.2.4"
  }
}
